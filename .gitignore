/target

# Keep .claude/skills/ tracked so they're versioned and shared; ignore everything else
# (worktrees, memory, local settings, session state).
.claude/*
!.claude/skills

# Local help to remember in which order to tackle tickets
docs/ticket-ordering*
