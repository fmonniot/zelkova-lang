# `src/Model.zel` and `tests/Model.zel` are both the module `Model`: one name, two
# files, which is the collision the two roots sharing one set of names produces.
name = "package-name-under-both-roots"
version = "0.1.0"
private-modules = []

[dependencies]

[test-dependencies]
