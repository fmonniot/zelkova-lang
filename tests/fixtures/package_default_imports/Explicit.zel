-- The same module, writing the default import out. It must mean the same thing
-- rather than registering `Basics` twice and making `+` ambiguous.
module Explicit exposing (y)

import Basics exposing (..)


y : Int
y = 1 + 2
