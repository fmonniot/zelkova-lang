-- Writing one of the eight default imports out by hand must mean the same thing
-- as leaving it implicit, rather than registering `Basics` twice and making `+`
-- ambiguous.
module Explicit exposing (y)

import Basics exposing (..)


y : Int
y = 1 + 2
